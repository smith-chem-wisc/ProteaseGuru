<UserControl x:Class="ProteaseGuruGUI.ProteinResultsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:ProteaseGuruGUI"    
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <DockPanel Grid.Row="0">
                    <Label Height="28" DockPanel.Dock="Left">Search</Label>
                    <TextBox Name="SearchTextBox" TextChanged="Search_TextChanged" DockPanel.Dock="Right"></TextBox>
                </DockPanel>

                <GroupBox Header="Proteins" Name="gbProteins" Grid.Row="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height=".9*"/>
                        </Grid.RowDefinitions>

                        <TreeView BorderThickness="0" x:Name="dataGridProteins" ItemsSource="{Binding}" DataContext="{x:Type local:ProteinForTreeView}"
                                SelectedItemChanged="proteins_SelectedCellsChanged" VirtualizingStackPanel.IsVirtualizing="True" 
                                  VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <TreeView.Resources>
                                <HierarchicalDataTemplate DataType="{x:Type local:ProteinForTreeView}" ItemsSource="{Binding Summary}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}" />
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                                <HierarchicalDataTemplate DataType="{x:Type local:SummaryForTreeView}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}" />
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.Resources>
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem">
                                    <Setter Property="IsExpanded" Value="{Binding Expanded}"/>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>
                    </Grid>
                </GroupBox>
                <Button Content="Load Proteins" Grid.Row="2" Width="100" Click="LoadProteins_Click" />
            </Grid>

            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Center"
                          Background="Silver"
                          Width="3"/>

            <GroupBox Grid.Column="2" Header="Sequence Coverage Maps" Name="gbPSM">
                <Grid Name="ResultsGrid" SizeChanged="resultsSizeChanged">
                    <Grid.RowDefinitions>
                        <RowDefinition Height = "30"></RowDefinition>
                        <RowDefinition Height = "*"></RowDefinition>
                        <RowDefinition Height = "60"></RowDefinition>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <ComboBox x:Name="ProteaseCoverageMaps" IsEnabled="True" Width="150" Height="30" 
                                   Text="Select Protease" SelectionChanged="proteaseComboBox_SelectionChanged" Loaded="proteaseCoverageMaps_loaded"/>

                    </Grid>
                    
                    <ScrollViewer Grid.Row="1" Name="mapViewer" HorizontalAlignment="Center" VerticalAlignment="Center" CanContentScroll="True" 
                                  HorizontalScrollBarVisibility="Visible">
                        <Grid Name="mapGrid" HorizontalAlignment="Center" Width="485">
                            <Canvas x:Name="map"/>
                        </Grid>
                    </ScrollViewer>

                    <!--legend for sequence coverage map-->
                    <Canvas Grid.Row="2" x:Name="legend" Width="500" HorizontalAlignment="Center" VerticalAlignment="Top">
                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Name="legendGrid">
                        </Grid>
                    </Canvas>
                </Grid>

            </GroupBox>
        </Grid>
    </Grid>
</UserControl>